/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.freepik.com',"i.ibb.co"], // Add any other domains you need here
      },
};

export default nextConfig;
